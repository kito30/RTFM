import SystemInfo from "./SystemInfo";
import { useSignalR } from "../context/signalRContext";

export default async function SysFetching() {
    const { connection, isConnected } = useSignalR();
    await
    return (
        <SystemInfo

        ></SystemInfo>  
    )
    
}