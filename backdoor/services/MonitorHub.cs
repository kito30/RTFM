using Microsoft.AspNetCore.SignalR;


namespace backdoor.services;
public class MonitorHub : Hub
{
    /// empty idk cause the user right now jsut query the data and connect to the hub
    /// the hub doenst actually send anything back
}